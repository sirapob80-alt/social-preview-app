'use client';
import {useState} from 'react'; import {useRouter} from 'next/navigation';
function makeRoomId(){const a='ABCDEFGHJKLMNPQRSTUVWXYZ23456789';return Array.from({length:6},()=>a[Math.floor(Math.random()*a.length)]).join('')}
export default function Home(){const router=useRouter();const[room,setRoom]=useState('');const join=()=>{const id=room.trim().toUpperCase().replace(/[^A-Z0-9]/g,'');if(id)router.push(`/room/${id}`)};return <main className="home"><section className="homeCard"><h1 className="logo">LinkRoom</h1><p className="sub">ห้องแชทสำหรับส่งลิงก์ Instagram, Facebook และ X พร้อมพรีวิวรูปใหญ่</p><button className="btn btnPrimary full" onClick={()=>router.push(`/room/${makeRoomId()}`)}>สร้างห้องใหม่</button><div className="row"><input className="field" value={room} onChange={e=>setRoom(e.target.value)} onKeyDown={e=>e.key==='Enter'&&join()} placeholder="ใส่รหัสห้อง เช่น AB12CD"/><button className="btn btnGhost" onClick={join}>เข้าห้อง</button></div><p className="note">ไม่ต้องสมัครสมาชิก แค่ตั้งชื่อแล้วคุยได้เลย</p></section></main>}
